# cp terraform.tfvars.example terraform.tfvars 后填真值（terraform.tfvars 已 gitignore）。
# VM 尺寸/地址/镜像都在 variables.tf 的默认值里（那才是真值，带推导注释），这里只放密钥。
# endpoint 不用写：justfile 的 plan/apply 走 SSH 隧道并用 -var 指定。
proxmox_password = "YourProxmoxRootPassword"
ssh_public_key   = "ssh-rsa AAAA... vg-gitlab"
