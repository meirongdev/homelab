apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: media

resources:
  - namespace.yaml
  - nfs-pv.yaml
  - limits.yaml
  - jellyfin.yaml
  - navidrome.yaml
  - podcast.yaml

# 镜像已在各清单里按 **manifest-list（OCI index）digest** 固定（仓库惯例）。
# homelab 是 amd64；镜像都是 multi-arch，留着 oracle arm64 迁移出口。
