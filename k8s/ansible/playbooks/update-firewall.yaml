- name: Update firewall rules on master nodes
  hosts: k8s_masters
  become: true
  tasks:
    - name: Allow Kubernetes API Server access (port 16443)
      community.general.ufw:
        rule: allow
        port: '16443'
        proto: tcp