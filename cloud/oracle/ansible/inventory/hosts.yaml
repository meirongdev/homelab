all:
  children:
    oracle_cluster:
      children:
        oracle_masters:
          hosts:
            oracle-k3s:
              ansible_host: <ORACLE_PUBLIC_IP>
          vars:
            ansible_user: ubuntu
            ansible_ssh_private_key_file: ~/.ssh/vgio
