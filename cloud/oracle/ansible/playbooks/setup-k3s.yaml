- name: Install prerequisites on Oracle K3s node
  hosts: oracle_masters
  become: true
  tasks:
    - name: Install prerequisites
      ansible.builtin.apt:
        name:
          - curl
          - git
          - python3-firewall  # required for ansible.posix.firewalld module
        state: present
        update_cache: true

- name: Configure OS for K3s
  hosts: oracle_masters
  become: true
  tasks:
    # OCI Ubuntu 24.04 uses firewalld (nftables backend) — not UFW.
    # OCI Security List handles external ingress at the cloud level;
    # firewalld handles it at the OS level. Both must allow K3s ports.
    - name: Allow K3s API port (6443) in firewalld
      ansible.posix.firewalld:
        port: 6443/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Allow HTTP (80) in firewalld
      ansible.posix.firewalld:
        port: 80/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Allow HTTPS (443) in firewalld
      ansible.posix.firewalld:
        port: 443/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    # K3s Cilium overlay needs pod/service CIDRs and interfaces trusted
    # in firewalld, otherwise the nftables filter_FORWARD chain will reject
    # pod egress traffic with "admin-prohibited".
    - name: Trust K3s pod CIDR (10.52.0.0/16) in firewalld
      ansible.posix.firewalld:
        source: 10.52.0.0/16
        zone: trusted
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Trust K3s service CIDR (10.53.0.0/16) in firewalld
      ansible.posix.firewalld:
        source: 10.53.0.0/16
        zone: trusted
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Trust cni0 interface in firewalld
      ansible.posix.firewalld:
        interface: cni0
        zone: trusted
        permanent: true
        state: enabled
      notify: Reload firewalld

    # Cilium VXLAN overlay interface (replaces flannel.1)
    - name: Trust cilium_vxlan interface in firewalld
      ansible.posix.firewalld:
        interface: cilium_vxlan
        zone: trusted
        permanent: true
        state: enabled
      notify: Reload firewalld

    # Cilium-specific ports
    - name: Allow Cilium health check (4240) in firewalld
      ansible.posix.firewalld:
        port: 4240/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Allow Cilium VXLAN overlay (8472) in firewalld
      ansible.posix.firewalld:
        port: 8472/udp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Allow Hubble Relay (4244) in firewalld
      ansible.posix.firewalld:
        port: 4244/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    - name: Allow ClusterMesh API server (32379) in firewalld
      ansible.posix.firewalld:
        port: 32379/tcp
        zone: public
        permanent: true
        state: enabled
      notify: Reload firewalld

    # OCI instances use iptables-legacy alongside nftables; set FORWARD ACCEPT.
    # 这条是**真正起作用且真正持久**的一条：policy 进 /etc/iptables/rules.v4
    # （`:FORWARD ACCEPT`），netfilter-persistent 开机恢复，Cilium 也不会改 policy。
    - name: Set FORWARD policy to ACCEPT (required for pod-to-pod routing)
      ansible.builtin.iptables:
        chain: FORWARD
        policy: ACCEPT

    # ⚠️ 2026-08-05 删掉了两条 `-A FORWARD -s 10.52.0.0/16 -j ACCEPT` /
    #    `-d 10.53.0.0/16 -j ACCEPT`。它们**永远无法收敛**，留着只会让
    #    `just check-node-drift`（ansible --check）永久报 2 条假 changed，
    #    把漂移检测变成噪音。完整证据链：
    #      1. rules.v4 里确实存了这两条，netfilter-persistent 开机 20:41:45 执行成功；
    #      2. 但 20:44 cilium-agent 起来后，`iptables -S FORWARD` 里只剩
    #         CILIUM_FORWARD 跳转和 wg0 两条 —— Cilium 重建 FORWARD 链时丢掉了外来规则
    #         （本集群 kube-proxy-replacement=true + enable-ipv4-masquerade=true，
    #          Cilium 全权管 iptables）；
    #      3. 而 FORWARD policy 本就是 ACCEPT（上一条任务），这两条纯属冗余。
    #    结论：pod↔pod 转发靠的是 policy ACCEPT + Cilium 自己的 CILIUM_FORWARD，
    #    不是这两条。要验证只需看 policy，`just verify-node` 已覆盖。

    - name: Persist iptables rules across reboots
      ansible.builtin.shell: |
        mkdir -p /etc/iptables
        iptables-save > /etc/iptables/rules.v4
      changed_when: true

    # Ubuntu 默认 fs.inotify.max_user_instances=128，root 进程（k3s/containerd/
    # 各 pod）会将其占满，导致依赖 inotify 的 pod 起不来——falco 曾因此
    # "could not initialize inotify handler" CrashLoop 23 天。homelab 节点为
    # 8192，此处保持一致。
    - name: Raise inotify instance limit (falco etc. need inotify handles)
      ansible.posix.sysctl:
        name: fs.inotify.max_user_instances
        value: '8192'
        sysctl_file: /etc/sysctl.d/99-inotify.conf
        state: present
        reload: true

    # OCI 的 Ubuntu 镜像带了一个 microk8s 装机残留：/etc/sysctl.d/20-microk8s-hugepages.conf
    # 里 vm.nr_hugepages = 1024，即 **2GiB 被锁成 hugetlb**。本机跑的是 k3s，从来没有
    # 任何 pod 申请过 hugepages（实测 HugePages_Free == HugePages_Total == 1024）。
    # 代价是实打实的：这 2GiB 直接从 node allocatable 里扣掉（capacity−allocatable 差额
    # 恰好等于它）。24GB 时占 8% 还能忍，2026-08-05 缩到 12GB 后占 17%，
    # 不清掉 allocatable 只剩 9.7GiB，低于实测 30d 内存峰值 10.2GiB → 一开机就进驱逐。
    - name: Remove leftover microk8s hugepages reservation (frees 2GiB)
      ansible.builtin.file:
        path: /etc/sysctl.d/20-microk8s-hugepages.conf
        state: absent

    - name: Zero hugepages (node capacity only refreshes on kubelet restart)
      ansible.posix.sysctl:
        name: vm.nr_hugepages
        value: '0'
        sysctl_file: /etc/sysctl.d/99-no-hugepages.conf
        state: present
        reload: true

    # ☠️ 2026-08-09：本节点 **5 天内非正常重启 3 次**（08-06 04:39、08-08 09:32、
    # 08-08 23:58，`last -x` 里都没有对应的 shutdown 记录），而每次都查不出原因 ——
    # 因为 journald 默认 `#Storage=auto`，且 `/var/log/journal` 不存在，
    # auto 在这种情况下退化为 **volatile（只写 /run，重启即蒸发）**。
    # 实测判据：`journalctl --list-boots` 只有 **1 条**记录，`journalctl -b -1` 空。
    # 对照组：两台 DGX 有持久 journal，所以 08-07 那串重启一登上去就能确认
    # 是内核升级链（有干净 shutdown），而 oracle 这边只能靠猜。
    # ⚠️ 只建目录还不够也不会立刻生效：Storage=auto 要**目录先存在**才落盘，
    # 且 journald 需要重启才会改用新路径 —— 故三步齐全（建目录 + 显式 persistent + 重启）。
    # 保留量给 200M：12GB 小盘，够覆盖数次重启的回溯窗口而不占地方。
    - name: Ensure persistent journal directory exists
      ansible.builtin.file:
        path: /var/log/journal
        state: directory
        owner: root
        group: systemd-journal
        mode: '2755'

    # ⚠️ 这一步不能省：OCI 的 Ubuntu 镜像**没有** /etc/systemd/journald.conf.d/
    # （只有 resolved.conf.d），而 ansible 的 copy 模块不会建父目录 ——
    # 2026-08-09 首次跑就是栽在这里：
    # `Destination directory /etc/systemd/journald.conf.d does not exist`。
    - name: Ensure /etc/systemd/journald.conf.d exists
      ansible.builtin.file:
        path: /etc/systemd/journald.conf.d
        state: directory
        owner: root
        group: root
        mode: '0755'

    - name: Configure journald to keep logs across reboots
      ansible.builtin.copy:
        dest: /etc/systemd/journald.conf.d/10-persistent.conf
        owner: root
        group: root
        mode: '0644'
        content: |
          # Managed by ansible (setup-k3s.yaml) —— 见该文件内注释里的重启复盘理由。
          [Journal]
          Storage=persistent
          SystemMaxUse=200M
      notify: Restart journald

    # ☠️ 2026-09-01：上面那段持久 journal 建成后**仍然**查不出重启原因 —— 因为证据
    # 根本到不了磁盘。真凶是内核 AppArmor 的 AF_UNIX 中介 bug：`unix_fs_perm()` 在
    # `mnt_idmap(path->mnt)` 处不检查 path->mnt 是否为空就解引用（vfsmount 偏移
    # 0x18 = mnt_idmap，与实测故障地址 0x18、故障指令 `ldr x0,[x0,#24]`、x0=0 逐位吻合）。
    # 而 `kernel.panic_on_oops` 在 Ubuntu oracle 内核里是**编译期默认 1**
    # （grep 遍 /etc/sysctl.d 与 /usr/lib/sysctl.d 都没有设它的文件），于是 oops 立刻
    # 升级成 panic → `kernel.panic=10` 十秒后硬重启，journald 来不及落盘 ——
    # 客场永远只看到"日志戛然而止"，这正是 2026-08-14 复盘误判成"宿主层硬重置"的原因。
    # 证据只存在于 hypervisor 侧：`oci compute console-history` 抓下来 1MB，
    # 内含 10 次 panic，签名 10/10 完全一致，崩溃进程全是 Node.js 的 libuv-worker。
    #
    # ⚠️ 这是**止血不是根治**：改 0 之后 oops 照旧发生，但只杀掉肇事线程、机器不再
    # 重启，把"每 1–4 天全集群断 6–10 分钟"降成"死一个 Node 线程"。
    # 代价：内核被标记污染（Tainted: G D）、每次泄漏少量 AppArmor label 引用计数
    # （1–4 天一次，可忽略），且这是**刻意偏离发行版默认**，别当成通用调优抄去别处。
    # ⚠️ 换内核逃不掉，别把工夫花在那上面：6.8 / 6.14 / 6.17 / 7.0 四条 oracle 轨道
    # **全都带**这段代码（实测比对 6.8.0-1060 与 6.17.0-1020 的 kallsyms，
    # unix_fs_perm / aa_unix_file_perm / aa_sock_file_perm 两边都在），
    # 且 v6.17 与上游 master 分支的那一行**完全相同** —— 补丁至今未进上游。
    # (terminology-ok: master 此处指 torvalds/linux 的 git 分支名，非控制面节点)
    # 根治只有两条：等上游修，或给触发方 pod 设 AppArmor Unconfined
    # （`unix_fs_perm` 开头就是 `if (unconfined(label)) return 0`，不受约束即不走该路径）。
    # 完整取证方法与推理链 → docs/records/2026-09-01-oracle-apparmor-af-unix-panic.md
    - name: Survive the AppArmor af_unix oops instead of hard-rebooting
      ansible.posix.sysctl:
        name: kernel.panic_on_oops
        value: '0'
        sysctl_file: /etc/sysctl.d/99-panic-on-oops.conf
        state: present
        reload: true

  # firewalld reload 是**动作**不是状态：写成普通任务的话每次 `--check` 都报 changed，
  # 两条就足以把 `just check-node-drift` 的信号淹掉。改 handler 后只在真有规则变更时触发。
  handlers:
    - name: Reload firewalld
      ansible.builtin.service:
        name: firewalld
        state: reloaded

    # ⚠️ 两步缺一不可（2026-08-09 实测踩过）：只 restart **不会**迁移到 /var。
    # 重启后 `journalctl --header` 仍指向 /run/log/journal、`/var/log/journal/` 空着，
    # 看起来配置生效了其实没有。journald 要收到 SIGUSR1 才会把 /run 刷到 /var
    # 并切换活动日志 —— 那正是 `journalctl --flush` 做的事。
    # （boot 时由 systemd-journal-flush.service 负责；但那时 /var/log/journal
    #  还不存在，所以它报 success 也没用，必须在建完目录后再刷一次。）
    - name: Restart journald
      ansible.builtin.service:
        name: systemd-journald
        state: restarted

    - name: Flush journal from /run to /var (journald 需 SIGUSR1 才切换存储)
      ansible.builtin.command: journalctl --flush
      changed_when: false

- name: Write K3s config file
  hosts: oracle_masters
  become: true
  tasks:
    - name: Create K3s config directory
      ansible.builtin.file:
        path: /etc/rancher/k3s
        state: directory
        mode: '0755'

    - name: Write /etc/rancher/k3s/config.yaml
      register: k3s_config
      ansible.builtin.copy:
        dest: /etc/rancher/k3s/config.yaml
        mode: '0644'
        content: |
          write-kubeconfig-mode: "644"
          disable:
            - servicelb
            - traefik
          node-name: oracle-k3s
          tls-san:
            # Tailscale IP — 唯一的外部 API 端点：ArgoCD 集群注册、本机 kubectl
            # (fetch-kubeconfig.yaml 写的就是 {{ ansible_host }}) 都经此。
            # 节点内网 IP (10.0.0.26) 由 k3s 自动加入证书 SAN，无需在此声明。
            #
            # 这里**刻意不含 OCI 公网 IP**（2026-08-10）：一是本仓库不提交公网地址
            # （CI: scripts/check-public-ips.py），二是没有任何客户端走公网连 6443。
            # 代价：`https://<公网IP>:6443` 会 TLS SAN 不匹配。破窗时不需要它——
            # SSH 进节点后本地 /etc/rancher/k3s/k3s.yaml 走 127.0.0.1。
            - 100.107.166.37
          cluster-cidr: "10.52.0.0/16"
          service-cidr: "10.53.0.0/16"
          flannel-backend: "none"
          disable-network-policy: true
          disable-kube-proxy: true
          # 2026-08-05 缩容到 2 OCPU / 12GB 时加。此前**一条 reserved 都没配**：
          # capacity−allocatable 的差额恰好等于那 2GiB hugepages，说明 kubelet 认为
          # 整机内存全部可分配给 pod。而 k3s-server 进程自己实测 RSS 2021Mi、
          # containerd 240Mi ——共约 2.3GiB 是调度器完全看不见的。24GB 上无所谓，
          # 12GB 上是整机 19%，不声明就等于允许调度器超卖到必然 OOM。
          #
          # memory=2560Mi：k3s 2021 + containerd 240 + tailscaled/sshd/journald 等约 200，
          #   再留一点余量。CPU 200m 同理（k3s-server idle 就要 ~150m）。
          # eviction-hard 500Mi：默认 100Mi 在 12GB 机器上太晚——到那一步内核 OOM killer
          #   往往已经随机挑了个受害者，kubelet 有序驱逐（按 QoS + Pod Priority）就没机会跑。
          kubelet-arg:
            - "system-reserved=cpu=200m,memory=2560Mi"
            - "eviction-hard=memory.available<500Mi,nodefs.available<10%"

    # 本 playbook **刻意不自动重启 k3s** —— 那等于在一条随手可跑的命令里重启整个集群。
    # 但 config.yaml 改了不重启就完全不生效，静默失效比报错更坑，所以这里显式喊一声。
    - name: Warn that config.yaml changes need a k3s restart to take effect
      ansible.builtin.debug:
        msg: >-
          config.yaml 已变更，但 k3s 未重启 —— kubelet-arg（system-reserved /
          eviction-hard）此刻**尚未生效**。改 shape 的场景请照
          docs/runbooks/oracle-k3s-shape-downsize.md 走（停机重启会一并生效）；
          只想立刻生效则手动 `sudo systemctl restart k3s`（全集群 pod 会重建）。
      when: k3s_config is changed

- name: Install K3s single node
  hosts: oracle_masters
  become: true
  vars:
    # ☠️ 必须钉版本，理由同 k8s/ansible/playbooks/setup-k3s.yaml：不钉 = 重建时
    # 装上 stable 频道当天的值，和现网差着 minor。本文件是 oracle-k3s 的重建路径
    # （docs/runbooks/oracle-k3s-rebuild.md），漏钉只在重建那天才爆。
    # 三处取值一致，由 scripts/check-version-pairs.py 的 V2 组 `k3s_version` 强制。
    # ⚠️ 描述现网、不是目标版本；升级流程见 docs/runbooks/k3s-cluster-upgrade.md。
    # renovate: datasource=github-releases depName=k3s-io/k3s
    k3s_version: v1.35.8+k3s1
  tasks:
    - name: Check if K3s is already installed
      ansible.builtin.stat:
        path: /usr/local/bin/k3s
      register: k3s_binary

    # 首装专用。升级不走这里，见 docs/runbooks/k3s-cluster-upgrade.md。
    - name: Install K3s (reads flags from /etc/rancher/k3s/config.yaml)
      ansible.builtin.shell: |
        curl -sfL https://get.k3s.io | \
          INSTALL_K3S_VERSION={{ k3s_version }} \
          sh -
      when: not k3s_binary.stat.exists
      changed_when: true

    - name: Wait for K3s to be ready
      ansible.builtin.command: k3s kubectl get nodes
      register: k3s_ready
      until: k3s_ready.rc == 0
      retries: 30
      delay: 10
      changed_when: false

- name: Install Gateway API CRDs (Cilium Gateway dependency)
  hosts: oracle_masters
  become: true
  vars:
    # ☠️ 与 k8s/helm/justfile 的 `gateway_api_version` 是同一个值，升 Cilium 时**两处同步改**。
    # CRD 版本与 Cilium 版本是一对：不满足时 operator 的 Gateway API 控制器**整个不初始化**，
    # 旧路由照常 200、新路由静默 503（2026-08-11 真实 30h 事故，
    # 见 docs/records/2026-08-11-gateway-api-crd-stall.md）。
    # 2026-08-13 前这里钉着 v1.2.1 且注释声称"与 homelab 一致"（实际 homelab 已 1.6.1）
    # ——按旧值重建就是原样重放事故。
    # renovate: datasource=github-releases depName=kubernetes-sigs/gateway-api
    gateway_api_version: "1.6.1"
  tasks:
    # k3s 内置 traefik 被 disable（见上面的 config），不再提供 Gateway API CRD。
    # 切勿改回让 traefik 托管这些 CRD —— disable traefik 会触发 helm-delete-traefik-crd
    # 把 CRD 一起删掉，导致 Gateway/HTTPRoute 级联回收、路由全断。
    # 见 docs/plans/networking/2026-06-04-oracle-k3s-argocd-gitops.md 事故复盘。
    #
    # 装法照抄 k8s/helm/justfile 的 deploy-gateway-api-crds：逐 CRD **server-side** apply
    # （整包 standard-install.yaml 走 client-side 会撞 last-applied 注解大小上限）。
    # ⚠️ TLSRoute 只在 experimental 频道发布，standard 频道没有，而 Cilium 要求它存在——
    # 只装 standard 包会重演"控制器不初始化"的同款 stall。
    # ⚠️ 对**已运行**的集群重跑本段后，需另行重启 cilium-operator（它只在启动时检查 CRD）；
    # 全新重建不需要（operator 在 CRD 之后才首次启动）。
    - name: Apply Gateway API standard CRDs (server-side)
      ansible.builtin.command:
        cmd: >-
          k3s kubectl apply --server-side --force-conflicts -f
          https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v{{ gateway_api_version }}/config/crd/standard/gateway.networking.k8s.io_{{ item }}.yaml
      loop:
        - gatewayclasses
        - gateways
        - httproutes
        - grpcroutes
        - referencegrants
        - backendtlspolicies
      changed_when: false   # server-side apply 的输出不区分新建/未变，幂等执行即可
    - name: Apply Gateway API experimental TLSRoute CRD (Cilium requires it)
      ansible.builtin.command:
        cmd: >-
          k3s kubectl apply --server-side --force-conflicts -f
          https://raw.githubusercontent.com/kubernetes-sigs/gateway-api/v{{ gateway_api_version }}/config/crd/experimental/gateway.networking.k8s.io_tlsroutes.yaml
      changed_when: false

- name: Configure additional DNS upstreams (redundancy for the 2026-08-01 outage mode)
  hosts: oracle_masters
  become: true
  tasks:
    # OCI VCN 通过 DHCP 只推一个上游 169.254.169.254 —— 它不可达时全集群外网 DNS 全挂，
    # cloudflared 随之崩（2026-08-01 outage ~20min，见 docs/records/2026-08-01-oracle-k3s-dns-outage.md）。
    #
    # ☠️ 必须用 DNS= 而不是 FallbackDNS=（2026-08-12 修正——此前那版是**无效修复**）：
    # FallbackDNS 只服务 resolved 自己的 stub(127.0.0.53) 路径，且**不会写进
    # /run/systemd/resolve/resolv.conf**；而 kubelet 把该文件喂给 CoreDNS
    # （`forward . /etc/resolv.conf`），即**集群路径**——当初真正死掉的那条——上游
    # 依旧只有 OCI 一个。FallbackDNS 版装完后 cloudflared 照样 26d 重启 24/28 次
    # （ROADMAP 原 #8 记录的"仍在发作"），就是这个原因。DNS= 是全局上游池，会进 resolv.conf。
    #
    # 只配 v4：节点无 v6 默认路由，v6 上游会成为 CoreDNS 永远探不通的死目标。
    #
    # ☠️ 下面这条原判断是**错的**，2026-08-13 已被一次真实故障推翻，**别照它的结论行事**：
    #   原文：「oraclevcn.com 内部名会有部分查询随机打到 1.1.1.1 → NXDOMAIN：集群无任何
    #          VCN 内部名依赖（cluster.local 由 kubernetes 插件应答），行为等价，可接受。」
    # 错在两处：
    #   (1) 公网递归解析器对 `*.vcn<id>.oraclevcn.com` 答的是 **SERVFAIL 而不是 NXDOMAIN**
    #       （该 zone 委派给 VCN 私有 NS，公网解析不到）。这两个 rcode 对解析器**不等价**。
    #   (2) 出问题的根本不是「VCN 内部名」，而是**所有外部域名**——OCI 的 DHCP 往
    #       resolv.conf 注入 `search vcn<id>.oraclevcn.com`，kubelet 把它接到 Pod 的
    #       search 列表里，于是 pod 每解析一个外部域名都会先探一发该后缀；musl 拿到
    #       SERVFAIL 会**放弃整轮 search**（NXDOMAIN 才继续），裸域名根本没被查过。
    # 结果：本 drop-in 落地次日（2026-08-13 03:30）的第一次夜间备份三连挂，
    # 且所有 Alpine 镜像的 pod 外网解析都变成了抽签。glibc 镜像不受影响。
    # 修法不在这里改（多上游冗余本身是对的、要保留），而是给 oraclevcn.com 单开一个
    # CoreDNS server 块只转发给 169.254.169.254 →
    #   cloud/oracle/manifests/base/coredns-custom.yaml（GitOps 同步，含完整机制说明）
    # 复盘：docs/records/2026-08-13-oracle-musl-dns-servfail.md
    #
    # 验证（2026-08-12 演练）：iptables 掐死 →169.254.169.254:53 共 30s，pod 内 4 个
    # 域名全部解析成功，DROP 计数 15 包——查询确实被掐、确实走了备胎。
    - name: Ensure /etc/systemd/resolved.conf.d exists
      ansible.builtin.file:
        path: /etc/systemd/resolved.conf.d
        state: directory
        mode: "0755"

    - name: Write additional DNS upstreams drop-in
      ansible.builtin.copy:
        dest: /etc/systemd/resolved.conf.d/10-oci-fallback.conf
        content: |
          [Resolve]
          DNS=1.1.1.1 1.0.0.1
        mode: "0644"
      register: dns_dropin

    - name: Restart systemd-resolved to apply
      ansible.builtin.systemd:
        name: systemd-resolved
        state: restarted
      when: dns_dropin.changed

    # CoreDNS 的 resolv.conf 由 kubelet 在 pod 创建时快照——resolved 改完必须重建
    # CoreDNS pod 才吃到新上游（不重建则一直用旧的单上游，"修了"跟没修一样）。
    - name: Recreate CoreDNS pod to pick up new upstreams
      ansible.builtin.command:
        cmd: k3s kubectl -n kube-system rollout restart deployment/coredns
      when: dns_dropin.changed

- name: Verify cluster
  hosts: oracle_masters
  become: true
  tasks:
    - name: Display cluster status
      ansible.builtin.command: k3s kubectl get nodes -o wide
      register: cluster_status
      changed_when: false

    - name: Show cluster status
      ansible.builtin.debug:
        msg: "{{ cluster_status.stdout_lines }}"
