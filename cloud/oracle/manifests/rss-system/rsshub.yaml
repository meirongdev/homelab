apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsshub
  namespace: rss-system
  labels:
    app: rsshub
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rsshub
  template:
    metadata:
      labels:
        app: rsshub
    spec:
      containers:
        - name: rsshub
          # 2026-08-04 digest 刷新（原 sha256:5bd34215…，已落后 upstream :latest）。
          # 动因：旧 digest 带 libgnutls30 CVE-2026-33845/42010 + protobufjs CVE-2026-41242。
          image: diygod/rsshub@sha256:b419ecc3227d68e1b15398fd29a75d921d67e34b2aa03dfd0d4d44a6f35213e8  # 当前 :latest 的多架构 index digest
          ports:
            - containerPort: 1200
              name: http
          env:
            - name: NODE_ENV
              value: production
            - name: CACHE_TYPE
              value: redis
            - name: REDIS_URL
              value: redis://rsshub-redis:6379/
            - name: PUPPETEER_WS_ENDPOINT
              value: ws://rsshub-browserless:3000
          resources:
            requests:
              cpu: 25m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /healthz
              port: 1200
            initialDelaySeconds: 30
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: 1200
            initialDelaySeconds: 15
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: rsshub
  namespace: rss-system
spec:
  type: ClusterIP
  selector:
    app: rsshub
  ports:
    - port: 1200
      targetPort: 1200
      name: http
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsshub-redis
  namespace: rss-system
  labels:
    app: rsshub-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rsshub-redis
  template:
    metadata:
      labels:
        app: rsshub-redis
    spec:
      containers:
        - name: redis
          image: redis:alpine
          ports:
            - containerPort: 6379
              name: redis
          resources:
            requests:
              cpu: 15m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: rsshub-redis
  namespace: rss-system
spec:
  type: ClusterIP
  selector:
    app: rsshub-redis
  ports:
    - port: 6379
      targetPort: 6379
      name: redis
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rsshub-browserless
  namespace: rss-system
  labels:
    app: rsshub-browserless
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rsshub-browserless
  template:
    metadata:
      labels:
        app: rsshub-browserless
    spec:
      priorityClassName: bulk
      containers:
        - name: browserless
          image: browserless/chrome@sha256:57d19e414d9fe4ae9d2ab12ba768c97f38d51246c5b31af55a009205c136012f  # 当前运行 digest 固定（原 :latest）
          ports:
            - containerPort: 3000
              name: http
          env:
            - name: MAX_CONCURRENT_SESSIONS
              value: "10"
          resources:
            requests:
              cpu: 25m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: rsshub-browserless
  namespace: rss-system
spec:
  type: ClusterIP
  selector:
    app: rsshub-browserless
  ports:
    - port: 3000
      targetPort: 3000
      name: http
