# Images — 自建容器镜像（Dockerfile 源）

`excalidraw-room/` 是 Excalidraw room-server 的自建镜像，由 CI 构建并推送
（`.github/workflows/excalidraw-room-image.yml`）。其余镜像均为上游官方镜像，不在此维护。

```
images/
└── excalidraw-room/
    └── Dockerfile
```
