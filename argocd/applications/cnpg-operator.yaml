apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cnpg-operator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: oracle-k3s
  sources:
    # CloudNativePG operator chart —— 部署前 `helm search repo cnpg/cloudnative-pg --versions` 核对。
    - repoURL: https://cloudnative-pg.github.io/charts
      chart: cloudnative-pg
      targetRevision: "0.29.0"   # operator appVersion 1.30.0
      helm:
        valueFiles:
          - $values/k8s/helm/values/cnpg-operator.yaml
    - repoURL: https://github.com/meirongdev/homelab
      targetRevision: main
      ref: values
  destination:
    # oracle-k3s 自身 —— 2026-08-02 ArgoCD 控制面迁入本集群后，这里是 in-cluster
    server: https://kubernetes.default.svc
    namespace: cnpg-system
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      # 大 CRD（CNPG Cluster CRD 很大）→ SSA 避免 client-side apply 的注解超长问题。
      - ServerSideApply=true
