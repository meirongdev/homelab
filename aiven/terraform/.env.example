# Copy to .env in this directory (gitignored, keep it 0600) and put the personal
# Aiven key there. Do NOT reuse the aiven CLI profile in ~/.config/aiven — on this
# machine that one is the company account.
AIVEN_API_TOKEN=your-aiven-api-key
