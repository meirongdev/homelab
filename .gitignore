# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files
*.tfvars
*.tfvars.json

# Ignore override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Terraform plan output
plan
plan.out
*.plan

# OS specific files
.DS_Store
.DS_Store?
._*

# Editor specific files
.vscode/
.idea/
*.swp
*.swo
*~

# Log files
*.log

# Environment variables
.env
!.env.example

# Vault keys
vault-keys.json

# Cloudflare credentials (NEVER commit)
cloudflare_credential.md

# Backup files
*.bak
*.tmp

# Merge conflict leftovers
*.orig

# Packer
credentials.pkr.hcl
variables.json
variables.auto.pkrvars.hcl

# Git worktrees
.worktrees/
**/vault_values.md

# OpenSpec / opsx AI-agent tooling — installed per-agent, not project content
.agent/
.codex/
.gemini/
openspec/
.claude/commands/
.claude/skills/openspec-*/
.qwen/commands/
.qwen/skills/
.github/prompts/
.github/skills/
proxmox/ansible/.vault_pass
.gstack/

# Agent 本地设置：允许列表里可能内联真实凭据（如 wrangler 命令带 CLOUDFLARE_API_TOKEN）。
# 此前只靠 ~/.config/git/ignore 这条全局规则挡着——换台机器/新克隆就没保护了，故在 repo 内固化。
.claude/settings.local.json
.qwen/settings.local.json
